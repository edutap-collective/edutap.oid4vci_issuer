"""Issues selective-disclosure verifiable credentials over OpenID4VCI.

The protocol lives in ``openid4vci``. This service supplies what that library
deliberately refuses to decide: which credential a given person may receive,
where the data comes from, which key signs it, and how long a challenge stays
valid.
"""
